# Задача для Claude Code: миграция Obsidian → Workspace

## Контекст
Семейная база знаний в Obsidian (`~/Dropbox/1  Малая Церковь/База Знаний/`) не используется. Переносим ценное в workspace OpenClaw (`~/.openclaw/workspace/`), где за файлами следит ИИ-ассистент Кузя.

## План
Подробный план миграции: `~/.openclaw/workspace/MIGRATION-PLAN.md`

## Инструкции

### 1. Прочитай план миграции целиком

### 2. Создай структуру папок
```
workspace/family/ (mission.md, faith/, meetings/, balance-wheel.md, housing/)
workspace/tea/ (notes/, catalog.md, inventory.md, courses.md, clients/)
workspace/yulia-blog/
workspace/finance/
workspace/books.md
```
`tea-notes/` переименовать в `tea/notes/`

### 3. Перенеси файлы по приоритету из плана

**Важно при переносе:**
- НЕ копировать слепо. Прочитай каждый файл, убери obsidian-специфичное (вики-ссылки `[[...]]`, фронтматтер если не нужен)
- Если информация уже есть в workspace (ARTUR.md, YULIA.md, EFREM.md, FAMILY.md, MEMORY.md) — НЕ дублировать, но перенести уникальное чего нет
- Профили людей (👥) — сверить с существующими файлами, дополнить если есть новое
- Маша и Алёна: интересы → добавить в FAMILY.md или создать отдельные файлы

### 4. НЕ переносить
- `🔧 Meta/` — инструкции для Obsidian, неактуальны
- `🏠 Кузя/` — уже в IDENTITY.md
- `📅 Спринты/` — заменены KANBAN.md
- `CLAUDE.md` — устарел
- `.obsidian/`, `.git/`, `.beads/`, `.claude/`

### 5. После миграции
- Удалить `MIGRATION-PLAN.md` и `MIGRATION-TASK.md`
- Добавить запись в `memory/2026-02-13.md`: «Миграция Obsidian → workspace завершена»

## Запуск
```bash
cd ~/.openclaw/workspace
claude
# скопировать эту задачу
```
